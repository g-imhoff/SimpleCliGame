package game.tools;

public class AStarAlgo {
    private int gCost, fCost, hCost;

    public AStarAlgo() { }

    public void initCost(Pos current, Pos dest) {
        int xDistance = dest.getX() - startPos.getX();
        int yDistance = dest.getY() - startPos.getY();
    }
}
